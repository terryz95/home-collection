import React from 'react';
import styles from './banner.module.scss';

export default function Banner() {
  const bannerUrl = ''
  const bannerSlogan = ''
  return <header>
    <h1>{ bannerSlogan }</h1>
  </header>
}
